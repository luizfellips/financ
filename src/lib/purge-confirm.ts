/** Phrase the user must type in the UI (step 2). */
export const PURGE_CONFIRM_PHRASE = "APAGAR TUDO";

/** Final API token (step 3 / request body). */
export const PURGE_CONFIRM_FINAL = "DELETE_ALL_DATA";
